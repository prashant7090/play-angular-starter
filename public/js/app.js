(function () {
    angular.module('sairat', [
        'ui.router'                    // Routing
    ])
})();

// Other libraries are loaded dynamically in the config.js file using the library ocLazyLoad11